[
<% @visores.each_with_index do |v,iv| -%>
{
  "imagem":"https://www.compreingressos.com/images/visores/<%= v.id %>/visor_mobile.jpg",
  "url":"<%= (v.link.include? "https") ? "#{v.link}":"https://www.compreingressos.com#{v.link}" %>",
  "descricao":<%=v.descricao.gsub('"','“').to_json%>
}<%= (@visores.size-1) <= iv ? '':',' %>
<% end -%>
]